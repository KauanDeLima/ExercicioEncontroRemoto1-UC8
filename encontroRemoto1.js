let listaPecas = ["Guidão", "Aro", "Selim", "Garfo","pastilha", "Freio", "Pneu", "Cambio", "Buzina", "Corrente"];

let nomePeca = "aro";
let pesoPeca = 450 ;

    if(listaPecas.length >= 10){ 

    console.log("Capacidade máxima atingido , limite de 10 peças ");

                 } else{ 
            if(pesoPeca >= 100){ 
            console.log("Peso da peça superior a 100 gramas, pode cadastrar.");

            if(nomePeca.length < 3){ 
                    
            console.log("alerta! O nome da peça deve ter mais que 3 caracteres.");

    }
  }
}